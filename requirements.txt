requests>=2.31

# SPECTER2 fingerprinting (build_fingerprints.py). The CUDA-specific torch
# build isn't reproducible via a plain `pip install -r requirements.txt` —
# these are for reference; the actual working env is ~/envs/hpca-matching.
numpy
torch
transformers
adapters
